import React from 'react';
import {Text, View} from 'react-native';

const Options: React.FC = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text>Hello Options Screen</Text>
    </View>
  );
};

export default Options;
